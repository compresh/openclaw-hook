{
  "id": "compresh",
  "name": "Compresh",
  "description": "Per-turn context compression — episodic memory architecture for LLM conversations",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "Compresh API key (sk-comp_...). Required for TUL 1.0 server enhancement. Without a key the plugin stays passive."
      },
      "endpoint": {
        "type": "string",
        "description": "Override the Compresh endpoint. Default: https://api.compre.sh/v1/tul1"
      },
      "protectionMode": {
        "type": "string",
        "enum": ["aggressive", "balanced", "conservative"],
        "description": "Number of trailing raw messages preserved without compression (2 / 4 / 8). Default: balanced."
      },
      "providerHint": {
        "type": "string",
        "description": "Reported in telemetry for per-provider stats. Example: anthropic, openai."
      },
      "modelHint": {
        "type": "string",
        "description": "Reported in telemetry for per-model stats. Example: claude-sonnet-4-5."
      },
      "minMessages": {
        "type": "integer",
        "minimum": 1,
        "description": "Skip compression below this message count. Default: 6."
      },
      "timeoutMs": {
        "type": "integer",
        "minimum": 1000,
        "description": "HTTP timeout for the /v1/tul1 call in milliseconds. Default: 8000."
      }
    }
  }
}
